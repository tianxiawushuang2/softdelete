<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.OrderMapper">
  <sql id="active">and delete_flag = 0</sql>
  <select id="queryById">select * from order_main where id = #{id} <include refid="active"/></select>
  <select id="queryWithoutFlag">select * from order_main where id = #{id}</select>
  <delete id="deleteById">delete from order_main where id = #{id}</delete>
  <select id="dynamicTableQuery">select * from ${tableName} where id = #{id}</select>
</mapper>
